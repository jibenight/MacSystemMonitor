name: Build

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5

      - name: Compiler (release)
        run: swift build -c release

      - name: Empaqueter l'app
        run: ./build_app.sh
